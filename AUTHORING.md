# תקן "סקיל הסייבורג" · מדריך לכתיבת סקיל חדש

מסמך זה הוא ספר החוקים למאגר `cyborg-skills`. המאגר מחזיק שלושה סוגי תוכן: **סקילים**
(כלי עבודה שרצים בתוך Claude Code), **מדריכים** (תוכן עריכתי בעברית שמלמד עבודה
אמיתית, ראו סעיף 7), ו**כלים מומלצים** (רשימה קצרה של כלי צד שלישי, ראו סעיף 8).
כל אחד מהשלושה עובר את `scripts/validate.mjs` ונדרש לעמוד בכל הכללים שכתובים כאן,
בלי חריגים. רוב המסמך (סעיפים 1 עד 6) עוסק בסקילים, שהם עדיין סוג התוכן המרכזי
והמורכב ביותר במאגר. הדוגמה המלאה ללימוד היא `skills/02-copy-content/hook-generator/`,
כדאי לפתוח אותה לצד המסמך הזה.

## 1. חוזה הפרונטמאטר

כל `SKILL.md` נפתח בבלוק YAML עם השדות הבאים:

- **name**: אנגלית קטנה בלבד, ספרות ומקפים (`a-z`, `0-9`, `-`), עד 64 תווים, זהה
  בדיוק לשם תיקיית הסקיל. אסור להכיל את המילים השמורות `anthropic` או `claude`
  ("cyborg" מותר). השם גם חייב להיות ייחודי בכל המאגר, לא רק בתוך הקטגוריה שלו: כל
  סקיל מותקן ל-`~/.claude/skills/<name>`, ושני סקילים בשם זהה ידרסו זה את זה אצל
  המשתמש. `scripts/validate.mjs` ו-`scripts/build-catalog.mjs` בודקים כפילויות שם
  על פני כל הקטגוריות.
- **description**: לא ריק, עד 1024 תווים. זה השדה היחיד בכל הריפו שמותר בו מקף
  מפריד, כמוסכמה של הריפו הזה (כך גם `templates/SKILL.md.template` כותב אותו). בכל
  מקום אחר בעברית, המקף המפריד אסור, מנסחים מחדש עם פסיק, נקודה או נקודתיים. זו לא
  רק מוסכמת סגנון: `npm run validate` בודק את זה כשגיאה קשה על גוף הסקיל (חוץ
  מבלוקים ומקטעי קוד), ונכשל אם מקף מפריד מופיע שם. אם סקיל צריך לצטט את התו עצמו
  כדי להסביר את הכלל, עוטפים אותו ב-code span עם backticks, לדוגמה: "בלי מקף מפריד
  ( `—` או `-` )".
- **license**: חייב להיות `MIT` בדיוק. אין חריגים.
- **metadata**: אובייקט עם אחד עשר שדות מחרוזת חובה: `title_he`, `category`,
  `category_slug`, `topic`, `level` (אחד מ-`beginner`, `intermediate`, `advanced`),
  `visibility` (`public` או `booster`), `author`, `site`, `version`, `summary_he`,
  `audience_he`. שדה `tags` אופציונלי: מחרוזת מופרדת בפסיקים, שהקטלוג הופך אוטומטית
  לרשימה. `category_slug` חייב להיות זהה בדיוק לשם תיקיית הקטגוריה שבה יושב הסקיל
  (למשל סקיל תחת `skills/02-copy-content/...` חייב `category_slug: "02-copy-content"`),
  אחרת `npm run validate` נכשל. בנוסף, `metadata.safety` הוא אובייקט חובה (לא
  מחרוזת) עם שלושה שדות בוליאניים: ראו "שדה `safety`" למטה.

  **`summary_he` ו-`audience_he` הם קופי שיווקי ללקוח, לא ל-Claude.** האתר
  הציבורי מציג אותם ישירות לבעל עסק שלא טכני, בכרטיס הסקיל ובראש דף הסקיל.
  זה שונה מהותית מ-`description`, שנכתב כדי שסוכן ידע מתי להפעיל את הסקיל
  (ולכן מכיל ביטויי הפעלה כמו "הפעל כש..." ואת "CLAUDE.md"), ומהגוף שנכתב
  כדי שאדם יבצע את הסקיל צעד אחר צעד.
  - **`summary_he`**: משפט אחד שאומר מה הלקוח **מקבל** בפועל (לא מה הסקיל
    "עושה" מנקודת מבט טכנית, ולא כמה אנחנו טובים). לדוגמה טובה: "מקבלים עשרה
    רעיונות לפתיחת מודעה, כל אחד בזווית שונה." אסור לו: לחזור על `title_he`
    (הכותרת כבר מוצגת מעל הכרטיס, חזרה עליה היא כפילות מיותרת), להכיל ביטוי
    הפעלה כמו "הפעל כש", או להזכיר `CLAUDE.md` (הערת מימוש שלא מעניינת לקוח).
    `npm run validate` בודק את כל אלה כשגיאה קשה, וכן בודק שאין בו מקף מפריד
    (ראו למטה) ושאינו עובר 320 תווים.
  - **`audience_he`**: שורה קצרה אחת שעונה "למי זה מתאים" במונחים עסקיים
    קונקרטיים, למשל "מתאים לבעל עסק שצריך לכתוב מודעה או פוסט ונתקע בפתיחה."
    `npm run validate` בודק שאין בו מקף מפריד ושאינו עובר 200 תווים.
  - **חשוב: `summary_he`/`audience_he` הם היוצא מן הכלל היחיד ההפוך.** בעוד
    ש-`description` הוא השדה היחיד בריפו שמותר בו מקף מפריד, `summary_he`
    ו-`audience_he` **אסורים** במקף מפריד בדיוק כמו גוף הסקיל, למרות שהם
    בתוך בלוק ה-YAML. אל תעתיקו את מוסכמת ה-`description` לשדות האלה.

  **שדה `site_gate` (אופציונלי, לא חלק מאחד עשר השדות החובה).** קובע איך האתר
  הציבורי מציג את הסקיל: `"open"` מציג את פקודת ההתקנה בגלוי, `"gated"` שם
  טופס הרשמה לרשימת תפוצה לפני פקודת ההתקנה. **ברירת המחדל היא פתוח.** תחת
  התפיסה הנוכחית של הריפו, אין כאן שכבות גישה ואין כאן שום דבר סודי, הריפו
  כולו ציבורי וכל אחד יכול לקחת כל סקיל ממנו בלי מגבלה. `site_gate` הוא בחירה
  שיווקית גרידא (איזה סקיל להציג עכשיו ברשת כדי לאסוף לידים), לא בקרת הרשאות.
  לכן שדה חסר, ריק, או עם ערך שגוי (טעות הקלדה, אות גדולה) **חייב** לגלוש
  לפתוח, לעולם לא לחסימה: `scripts/lib/catalog.mjs` מממש את זה כך שרק
  המחרוזת המדויקת `"gated"` נועלת סקיל, כל ערך אחר, כולל שדה חסר לגמרי, הופך
  ל-`"open"`. בגלל ברירת המחדל הבטוחה הזו, `npm run validate` **לא** דורש את
  השדה ולא נכשל אם הוא חסר. אם הוא כן קיים אבל עם ערך לא מוכר, זו אזהרה בלבד
  (אדם קורא ומתקן), בדיוק כמו רשימת הדגלים הבטיחותיים בסעיף 3 למטה. הפיכת סקיל
  בודד לחסום היא שינוי שורה אחת בקובץ `SKILL.md` שלו בלבד
  (`site_gate: "gated"`), בלי שום שינוי באתר. שלושת הסקילים החיים כרגע כתובים
  במפורש כ-`"open"`, זו בחירה מפורשת של אלעד.

  **שדה `safety`**: אובייקט (לא מחרוזת) עם שלושה שדות בוליאניים חובה:
  `writes_files`, `sends_external`, `touches_live_campaigns`. האתר הופך אותו
  למשפט בטיחות אמיתי לכל סקיל, במקום הבטחה גורפת אחת שמוצגת על כל הסקילים
  (והופכת לשקר ברגע שסקיל אחד כן כותב או שולח משהו). קובעים כל ערך **לפי
  קריאה בפועל** של גוף ה-`SKILL.md` של הסקיל עצמו, לא לפי הנחה:
  - `writes_files: true` אם יש בגוף הסקיל שורה שמורה לכתוב קובץ לדיסק (למשל
    "כתוב קובץ בשם..."), גם אם זה מרגיש תמים כמו קובץ טקסט זמני.
  - `sends_external: true` אם הסקיל עצמו שולח משהו החוצה בפועל (מייל, הודעה,
    פרסום, קריאת רשת יוצאת) כפעולה שהוא מבצע. זה לא כולל את המשתמש שמריץ בעצמו
    מתקין חיצוני (כמו Homebrew) שהסקיל רק הנחה אותו להתקין, כי שם הקריאה
    לרשת היא של המשתמש ושל התוכנה החיצונית, לא של הסקיל. הקו הזה עדין, אז
    בכל ספק תארו במפורש ב-`SKILL.md` מה בדיוק קורה, כדי שהערך יהיה נכון גם
    בלי לדעת את ההבחנה הזו.
  - `touches_live_campaigns: true` אם הסקיל נוגע, מפעיל, עוצר או משנה קמפיין
    חי בפועל (לא רק מייצר טיוטה או המלצה).

  דייקו. משפט הבטיחות באתר נשען על הערכים האלה בעיני זר שלא בדק בעצמו.

`scripts/validate.mjs` בודק את כל השדות האלה ונכשל אם משהו חסר או לא תקין. הרצה:
`npm run validate`.

## 2. מבנה הגוף הקבוע

לכל סקיל אותו שלד, בעברית:

1. **חתימת מותג, ראשונה תמיד.** הוראה מפורשת בתחילת הסקיל להדפיס:
   ```
   ============================================================
    ⚡ The Cyborg · <title_he>
    https://thecyborg.co.il
   ============================================================
   ```
   `validate.mjs` בודק שהמחרוזת `"The Cyborg ·"` קיימת בגוף הסקיל.
2. **שלב 0, קריאת CLAUDE.md.** קורא את קובץ ה-CLAUDE.md של העסק אם קיים, ושואב ממנו
   זהות, קהל וטון. אם אין קובץ כזה, הסקיל אומר זאת בקצרה וממשיך לעבוד גם בלעדיו. הסקיל
   מותקן על מחשב זר של מי שהוריד אותו כמגנט ליד, ואסור שיהיה תלוי בהקשר שאין לו.
3. **שלבי עבודה.** צעד אחר צעד, קצר וברור, בלי פטפוט מיותר.
4. **כללי איכות.** הערך ללקוח במרכז, עברית טבעית ותקינה, בלי מקף מפריד, לא להמציא
   נתונים (אם משהו לא מבוסס, לסמן "לאמת").
5. **הצעד הבא.** הזמנה עדינה חזרה לתוכנית הליווי של הסייבורג, לא מכירה כבדה.

## 3. כלל הבטיחות

כל סקיל הוא read-only / draft-only כברירת מחדל: הוא מייצר טיוטות, ניתוחים והמלצות
בלבד. אסור לו למחוק קבצים, לשלוח החוצה בפועל (מייל, הודעה, פרסום), להריץ פקודות
מסוכנות, או לגעת בקמפיינים חיים. אם סקיל בכל זאת דורש כלי חיצוני (Bash, גישת רשת),
זה מוצהר במפורש ומוסבר ב-`INSTALL.md` שלו.

`scripts/lib/validate-skill.mjs` סורק את גוף הסקיל גם מול רשימת דגלים בטיחותיים
(`rm -rf`, `git push`, קריאת `curl`/`fetch` לכתובת חיצונית, הוראה לשלוח מייל). פגיעה
ברשימה הזו היא **אזהרה, לא שגיאה**: `npm run validate` עדיין מדפיס ✅ ומחזיר קוד
יציאה 0, אבל האזהרה מודפסת כדי שאדם יקרא ויחליט, לא כדי לחסום אוטומטית.

בנוסף, המפעיל יכול להחזיק קובץ מקומי בשם `forbidden-markers.local.json` בשורש
הריפו, עם רשימת מקורות שסקיל אסור להיגזר מהם. הקובץ הזה לא מחובר לגיט ולעולם לא
מתועד כאן בשמות. אם הוא קיים, `scripts/validate.mjs` ו-`scripts/build-catalog.mjs`
טוענים אותו וסורקים כל סקיל מולו, ממש כמו רשימת הדגלים הבטיחותיים למעלה: התאמה
מציפה אזהרה לבדיקה אנושית, אף פעם לא שגיאה חוסמת. האזהרות האלה הן המלצה בלבד,
לפעמים ההתאמה תיפול על מילה תמימה לגמרי, ותמיד בן אדם הוא זה שמחליט אם יש בעיה
אמיתית.

## 4. כללי ברזל

- **מקוריות מהיסוד בלבד.** כל סקיל נכתב מאפס לפי המתודה המקורית של הסייבורג. לעולם לא
  נגזר, מועתק, ואפילו לא נפתח לצד שיטה חיצונית או חומר של צד שלישי כרפרנס.
- **בלי IP פנימי של הסייבורג.** הריפו ציבורי. אסור להכניס אליו חומר פנימי, תמחור,
  נהלים ארגוניים, או תוכן רגיש של לקוחות.
- **MIT לכל סקיל**, בדיוק כמו הריפו כולו.

## 5. תהליך העבודה של המפעיל

1. `npm run new-skill name=... category_slug=... category="..." topic="..." title_he="..." level=... one_liner="..." trigger="..." step1_title="..." example_prompt="..." summary_he="..." audience_he="..." writes_files=false sends_external=false touches_live_campaigns=false [tags="..."]`

   כל הארגומנטים האלה חובה, חוץ מ-`tags` שהוא אופציונלי. הסקריפט (`scripts/new-skill.mjs`)
   מסרב לרוץ אם `writes_files`/`sends_external`/`touches_live_campaigns` אינם בדיוק
   `true` או `false`. הסקריפט יוצר תיקייה חדשה תחת `skills/<category_slug>/<name>/`
   עם `SKILL.md` ו-`INSTALL.md` כבר ממולאים מהתבנית (`templates/SKILL.md.template`,
   `templates/INSTALL.md.template`). שימו לב: בשלב הזה עדיין לא כתבתם את גוף הסקיל,
   אז ערכי `writes_files`/`sends_external`/`touches_live_campaigns` שהזנתם כאן הם
   ניחוש ראשוני בלבד. חובה לחזור ולוודא אותם בסוף מול הגוף שכתבתם בפועל (שלב 2 למטה),
   ולתקן ב-`SKILL.md` אם התברר שהם לא מדויקים.
2. **כתיבת הגוף.** משלימים ידנית את שלבי העבודה (שלב 1 ואילך) בקובץ `SKILL.md`, לפי
   המתכון בסעיף 2 למעלה. בסיום, לוודא ש-`summary_he`, `audience_he` ו-`safety`
   בפרונטמאטר עדיין מדויקים ביחס לגוף הסופי שנכתב.
3. `npm run validate`: מריצים עד שהסקיל מקבל ✅. כל שגיאה מודפסת עם הסיבה המדויקת שלה.
4. `npm run build`: בונה מחדש את `catalog.json` ואת `CATALOG.md` מכל הסקילים
   התקינים במאגר. אם סקיל כלשהו לא תקין, גם אם הוא לא הסקיל שכתבת עכשיו, הסקריפט
   מסרב לכתוב קטלוג בכלל ומפנה להריץ קודם `npm run validate`.
5. מציגים לאלעד לאישור. בלי push ל-GitHub ובלי deploy, אף פעם, בלי אישור מפורש שלו.

## 6. סקיל לדוגמה

`skills/02-copy-content/hook-generator/` הוא הדוגמה המלאה. אפשר להעתיק ממנה מבנה
וסגנון כתיבה, אבל לא תוכן: כל סקיל חדש נכתב מהמתודה המקורית של הנושא שלו.

## 7. מדריכים (guides)

מדריך הוא תוכן עריכתי בעברית שעומד בפני עצמו: מלמד קורא לא טכני לבצע עבודה אמיתית
צעד אחר צעד (למשל "איך כותבים עשרה הוקים למודעה בעשרים דקות"), ובדרך כלל, אבל לא
תמיד, קשור לסקיל אחד שממנו הקורא יכול גם להתקין ולהמשיך לבד. **מדריך הוא לא
`INSTALL.md`.** ל-`INSTALL.md` יש תפקיד אחר וקבוע: הוראות התקנה טכניות קצרות לסקיל
ספציפי, והוא נשאר בדיוק איפה שהוא. מדריך הוא תוכן שיווקי לכל דבר, נכתב כדי שאדם ירצה
לקרוא אותו וירגיש שהוא יצא ממנו עם ערך אמיתי, לא רשימת הוראות.

**מבנה קובץ.** כל מדריך יושב ב-`guides/<name>/GUIDE.md`, עם פרונטמאטר YAML שטוח
(בניגוד לסקיל, בלי בלוק `metadata` מקונן, כי אין למדריך תיאור פונה-לסוכן ואין לו
את השדות המרובים של סקיל):

```yaml
---
name: kebab-case-id
title_he: "..."
summary_he: "משפט אחד פונה ללקוח"
category: "..."
category_slug: "..."
related_skill: hook-generator   # שם סקיל קיים, או null (בלי מרכאות) אם אין
license: MIT
---
```

- **name**: אנגלית קטנה, ספרות ומקפים בלבד, זהה בדיוק לשם תיקיית המדריך, וכפוף לאותה
  רשימת מילים שמורות (`anthropic`, `claude`) כמו סקיל. **בניגוד לסקיל, השם לא צריך
  להיות ייחודי מול שמות הסקילים** (מדריכים ותיקיית `guides/` הם מרחב שמות נפרד
  מ-`skills/`), אבל כן חייב להיות ייחודי בין המדריכים עצמם.
- **title_he, category, summary_he**: קופי פונה ללקוח, בדיוק כמו `summary_he` של סקיל:
  בלי מקף מפריד (`—` או `–`), `summary_he` עד 320 תווים ואסור לו לחזור על `title_he`.
- **category_slug**: מחרוזת חופשית, לא נבדקת מול שם תיקייה (למדריכים אין תת תיקיות
  קטגוריה כמו לסקילים), אבל מקובל להשתמש באותם slugים שהסקילים כבר משתמשים בהם
  (`02-copy-content` וכו') כדי לשמור על טקסונומיה אחידה באתר.
- **related_skill**: שדה חובה, גם אם אין קשר לאף סקיל, ואז כותבים את הערך הגולמי
  `null` (בלי מרכאות). אם יש ערך, הוא חייב להיות שם של סקיל שקיים בפועל במאגר.
  הוא גם **חייב להופיע כטקסט בגוף המדריך עצמו** (למשל בתוך פקודת ההתקנה שמוצגת
  לקורא): מדריך שמצהיר על קשר לסקיל אבל לא מזכיר אותו נכשל בבדיקה.
- **license**: `MIT`, בדיוק כמו סקיל.
- **highlight** (אופציונלי, בוליאני): מציין מדריך שכדאי להבליט באתר, כי קורא
  יוצא נפסד אם הוא מדלג עליו (למשל מדריך הבטיחות). ברירת המחדל היא `false`:
  `scripts/lib/catalog.mjs` בודק `=== true` בדיוק, כך ששדה חסר, ריק, או עם
  ערך לא תקין הופך למדריך רגיל בלי הבלטה, לעולם לא לשגיאה. `npm run validate`
  לא דורש את השדה.

**גוף המדריך.** אין מבנה קבוע של סעיפים (מדריך הוא לא סקיל, אף סוכן לא "מריץ" אותו
צעד אחר צעד), אבל שתי דרישות טכניות: שורת כותרת ראשונה שמתחילה ב-`# ` (H1), והגוף
לא יכול להיות ריק. מעבר לזה, זה נכתב כמו מאמר: כותב אנושי, פונה לבעל עסק לא טכני,
מוביל עבודה אמיתית ולא מתאר "מה הסקיל עושה". אם המדריך קשור לסקיל, שווה לכלול בתוכו
את פקודת ההתקנה בפועל (`npx degit .../<category_slug>/<name> ~/.claude/skills/<name>`),
בדיוק כמו שמופיעה ב-`INSTALL.md` של אותו סקיל, כדי שהקורא יוכל להתקין בלי לצאת
מהמדריך. כללי האיכות זהים לסקיל: הערך ללקוח במרכז, עברית טבעית ותקינה, בלי מקף
מפריד, לא להמציא נתונים. בנוסף, בגלל שהריפו ציבורי, גוף המדריך אסור שיכיל נתיב
מילולי שמתחיל ב-`/Users/`.

**תהליך.** `npm run new-guide name=... category_slug=... category="..." title_he="..." summary_he="..." related_skill=hook-generator` (או `related_skill=null`)
יוצר את `guides/<name>/GUIDE.md` מהתבנית (`templates/GUIDE.md.template`). כותבים את
הגוף בפועל, מריצים `npm run validate` עד ירוק, ואז `npm run build`. `scripts/lib/validate-guide.mjs`
בודק את כל הכללים למעלה ומדפיס שגיאה מדויקת לכל כשל, בדיוק כמו `validate-skill.mjs`.

## 8. כלים מומלצים (links)

כלי מומלץ הוא המלצה על כלי חוץ (SaaS, אפליקציה, שירות) שרלוונטי לבעל עסק קטן.
**לכלי אין דף משלו באתר**, הוא שורה אחת ברשימה מאוצרת. מקור האמת היחיד לרשימה הוא
קובץ JSON שטוח אחד, `links/links.json`: מערך של אובייקטים, כל אחד בדיוק בצורה הזו:

```json
{
  "name": "kebab-case-id",
  "title": "Tool Name",
  "url": "https://...",
  "summary_he": "משפט אחד פונה ללקוח: מה בעל עסק מקבל מהכלי הזה",
  "group": "video" | "text",
  "pricing_he": "הערה קצרה וכנה על מחיר",
  "hebrew_support": "full" | "partial" | "none"
}
```

- **name**: כמו בסקיל, אנגלית קטנה ומקפים בלבד, וייחודי בין הכלים ברשימה.
- **url**: חייב להיות כתובת מלאה (לא יחסית) עם `https`, לא `http`.
- **summary_he, pricing_he**: קופי פונה ללקוח בעברית, בלי מקף מפריד, עד 320 ו-200
  תווים בהתאמה. `pricing_he` צריך להיות כן: אם יש מסלול חינמי מוגבל, לכתוב את זה,
  לא רק את המחיר הכי יקר.
- **group**: כרגע רק `"video"` או `"text"`, יתרחב כשיתווספו קבוצות כלים נוספות.
- **hebrew_support**: `"full"`, `"partial"` או `"none"`, לפי בדיקה בפועל של הממשק,
  לא ניחוש.

**תהליך.** אין סקריפט scaffolding לכלים (זו רשימה שטוחה, לא תיקייה): פשוט מוסיפים
אובייקט חדש למערך ב-`links/links.json` ומריצים `npm run validate`.
`scripts/lib/validate-links.mjs` בודק את הצורה של כל שדה, כולל שה-URL מוחלט ו-https,
ומדפיס שגיאה מדויקת לכל כשל. **חשוב**: הרשימה הנוכחית ריקה (`[]`) בכוונה. תוכן
הרשימה נחקר בנפרד וטעון אישור של אלעד לפני שהוא נכנס, כך שרשימה ריקה תקינה
עדיפה על פני כלים לא מאומתים.
