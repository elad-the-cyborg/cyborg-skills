# cyborg-skills

מאגר ציבורי של Agent Skills מקוריים בעברית, של **The Cyborg**, סוכנות שיווק ממודיעין.
כל סקיל הוא כלי עבודה קטן ועצמאי שרץ בתוך קלוד קוד: קורא הקשר על העסק שלך (מתוך
CLAUDE.md, אם יש כזה), ומחזיר תוצר שיווקי מוכן לשימוש, בלי צורך בידע טכני.

המאגר משרת שתי מטרות. הוא **מגנט לידים**: כל סקיל אפשר להתקין בחינם ולקבל ממנו ערך
מיידי, בלי מחויבות. והוא מיועד לשמש גם **תשתית תוכן** לתוכנית הליווי של הסייבורג:
התכנון הוא שמדף הסקילים בפורטל המשתתפים יישאב ממנו, כך שעדכון סקיל כאן ישתקף בכל
הערוצים ברגע שהחיבור ייבנה (פרטים בהמשך).

## כללי ברזל

- **מקוריות מהיסוד.** כל סקיל נכתב מאפס, לפי המתודה המקורית של הסייבורג בלבד.
- **בלי IP פנימי.** הריפו ציבורי, בלי חומר פנימי או רגיש של הסייבורג או של לקוחותיו.
- **MIT** לריפו ולכל סקיל בתוכו, ראו [`LICENSE`](LICENSE).
- **טיוטות בלבד, ובלי הפתעות.** אף סקיל במאגר לא מוחק קבצים, לא שולח מייל או
  הודעה בשמכם, ולא נוגע בקמפיין חי. חלק מהסקילים כן כותבים קבצים חדשים על
  המחשב (למשל קובצי התמונה של קרוסלה), וחלק כן פותחים כתובות אינטרנט כדי לקרוא
  אותן. לכל סקיל יש שלושה דגלים משלו, `writes_files`, `sends_external`
  ו-`touches_live_campaigns`, שאומרים בדיוק מה הוא עושה. הם מופיעים
  ב-[`CATALOG.md`](CATALOG.md) ובכל `SKILL.md`, וכדאי להסתכל בהם לפני התקנה.

## איך מתקינים סקיל

לכל סקיל יש `INSTALL.md` משלו עם הסבר מלא בעברית פשוטה, אבל התבנית זהה לכולם.

**שורה אחת (מומלץ), העתק והדבק:**
```
npx degit elad-the-cyborg/cyborg-skills/skills/<category_slug>/<name> ~/.claude/skills/<name>
```
לדוגמה, כדי להתקין את מחולל ההוקים:
```
npx degit elad-the-cyborg/cyborg-skills/skills/02-copy-content/hook-generator ~/.claude/skills/hook-generator
```

**חלופה ידנית:** מורידים את תיקיית הסקיל הבודדת מתוך `skills/<category_slug>/<name>/`
וגוררים אותה אל `~/.claude/skills/` במחשב. בשני המקרים צריך Claude Code מותקן מראש
(VS Code מומלץ).

## מפת הקטגוריות

המאגר בנוי סביב 7 קטגוריות ו-21 נושאים, סקיל דגל אחד לכל נושא.

| # | קטגוריה | נושאים | סטטוס |
|---|---------|--------|-------|
| 1 | מחקר ואסטרטגיה | מחקר מתחרים, נראות בחיפוש ובבינה מלאכותית, רושם ראשוני באתר | 3 סקילים חיים: [`competitor-radar`](skills/01-research-strategy/competitor-radar), [`findability-audit`](skills/01-research-strategy/findability-audit), [`first-impression-audit`](skills/01-research-strategy/first-impression-audit) |
| 2 | קופי ותוכן | הוקים וזוויות, קופי למודעות, קופי לדף נחיתה | 5 סקילים חיים: [`hook-generator`](skills/02-copy-content/hook-generator), [`meta-ads-copy`](skills/02-copy-content/meta-ads-copy), [`taboola-ads-copy`](skills/02-copy-content/taboola-ads-copy), [`google-search-ads-copy`](skills/02-copy-content/google-search-ads-copy), [`landing-page-copy`](skills/02-copy-content/landing-page-copy) |
| 3 | קריאייטיב וויזואל | קרוסלות לאינסטגרם, עריכת וידאו לרשתות חברתיות, פלטת מותג וקונספט קריאייטיב | 2 סקילים חיים: [`instagram-carousel`](skills/03-creative-visual/instagram-carousel), [`video-editing`](skills/03-creative-visual/video-editing) |
| 4 | דפי נחיתה ובנייה | בניית דף נחיתה, בניית אפליקציות, בקרת המרות | בתכנון |
| 5 | קמפיינים ומדיה | מבנה קמפיין מטא, תוכנית מדיה, ניתוח ביצועים | בתכנון |
| 6 | SEO / AEO / GEO | מחקר מילות מפתח, אופטימיזציית עמוד, נראות ב-AI | בתכנון |
| 7 | אוטומציה ובקרה | בקרת איכות, אבטחה בסיסית, ארגון דאטה | בתכנון |

הקטלוג המלא והמעודכן תמיד ב-[`CATALOG.md`](CATALOG.md) (טבלה קריאה לאדם)
וב-[`catalog.json`](catalog.json) (למכונה).

## מה עוד יש כאן חוץ מסקילים

- **מדריכים** (`guides/`): תוכן קריאה בעברית פשוטה שמלמד עבודה אמיתית מקצה
  לקצה, בלי להתקין כלום. חלקם צמודים לסקיל שאפשר להתקין בסוף הקריאה, וחלקם
  עומדים לבד.
- **כלים מומלצים** (`links/links.json`): רשימה קצרה ומאוצרת של כלי צד שלישי
  שמתאימים לעסק קטן, כל אחד עם מה מקבלים ממנו, מה הוא עולה באמת, ועד כמה הוא
  באמת מסתדר עם עברית.

שניהם מופיעים ב-[`CATALOG.md`](CATALOG.md) וב-[`catalog.json`](catalog.json)
לצד הסקילים.

## איך הקטלוג מזין את שאר המערכת

`catalog.json` נבנה אוטומטית מהפרונטמאטר של כל הסקילים על ידי `npm run build`, והוא
כתובת האינטגרציה היחידה של המאגר. התכנון הוא שהוא יזין שני צרכנים: הסאב-דומיין
המגודר `skills.thecyborg.co.il`, שכרטיסי הקטלוג בו נבנים ישירות ממנו בלי תחזוקה
ידנית, ואת מדף הסקילים בפורטל תוכנית הליווי, שקורא אותו דרך ISR ומתעדכן אוטומטית
בכל שינוי. שני החיבורים האלה עוד לא נבנו, זה השלב הבא בתוכנית.

## עבודה על המאגר (למפעיל)

תהליך כתיבת סקיל חדש מתועד במלואו ב-[`AUTHORING.md`](AUTHORING.md): חוזה
הפרונטמאטר, מבנה הגוף הקבוע, וכללי הברזל. בקצרה:

```
npm run new-skill name=... category_slug=... category="..." topic="..." title_he="..." level=... one_liner="..." trigger="..." step1_title="..." example_prompt="..." summary_he="..." audience_he="..." writes_files=false sends_external=false touches_live_campaigns=false [tags="..."]
```
יוצר תיקיית סקיל חדשה מהתבנית. אחרי כתיבת הגוף:

```
npm run validate   # מוודא שכל סקיל במאגר עומד בתקן
npm run build       # בונה מחדש את catalog.json ו-CATALOG.md, מסרב אם סקיל כלשהו לא תקין
npm test            # מריץ את בדיקות הכלים עצמם (scripts/lib)
```

סקיל חדש נבנה תמיד דרך המפעל, `factory/skill-factory/`, שמרכז את התהליך הזה לכל
בקשה של "תבנה סקיל חדש".

## רישיון

MIT, ראו [`LICENSE`](LICENSE). כל סקיל במאגר מפורסם תחת אותו רישיון.
